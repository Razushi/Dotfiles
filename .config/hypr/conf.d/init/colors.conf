# Autoload the palette generated for Hyprlock so Hyprland can reuse it.
source = /home/razushi/.config/hypr/hyprlockColors.conf

# Hyprland expects 8 hex digits for rgba(). Provide the alpha-suffixed variant.
$surface_container_low_rawaa = 191c20aa
